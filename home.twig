{% extends "layout.twig" %}
{% block content %}

    {{widget('feature', 'featurehome', {
        'header': 'profile',
        'strapline': 'profile',
        'defaultBgImg': asset("images/feature-bg.jpg"),
        'positionTopPercentage': '0',
        'showBtn': false
    })|raw}}

    {{zone('main')|raw}}

{% endblock %}
